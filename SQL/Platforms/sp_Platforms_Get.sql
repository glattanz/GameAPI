SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE OR ALTER PROCEDURE dbo.sp_Platforms_Get
	@Id int
AS
BEGIN

	SET NOCOUNT ON;

	SELECT
		Id,
        Name
    FROM Platforms WITH(NOLOCK)
    WHERE Id = @Id
    
END