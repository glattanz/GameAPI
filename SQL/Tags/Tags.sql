SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

IF EXISTS (SELECT 1 FROM SysObjects WHERE Name = 'Tags')
BEGIN
	--* drop table dbo.Tags
    RETURN
END

CREATE TABLE dbo.Tags
(
    Id int IDENTITY(1,1) NOT NULL,
    Name nvarchar(256) NOT NULL
) ON [PRIMARY]

ALTER TABLE dbo.Tags ADD CONSTRAINT PK_Tags PRIMARY KEY CLUSTERED
(
    Id ASC
)
WITH (
    PAD_INDEX = OFF,
    STATISTICS_NORECOMPUTE = OFF,
    SORT_IN_TEMPDB = OFF,
    IGNORE_DUP_KEY = OFF,
    ONLINE = OFF,
    ALLOW_ROW_LOCKS = ON,
    ALLOW_PAGE_LOCKS = ON
) ON [PRIMARY]

CREATE NONCLUSTERED INDEX IDX_Tags_Id ON dbo.Tags
(
	Id ASC
)WITH (STATISTICS_NORECOMPUTE = OFF, DROP_EXISTING = OFF, ONLINE = OFF) ON [PRIMARY]